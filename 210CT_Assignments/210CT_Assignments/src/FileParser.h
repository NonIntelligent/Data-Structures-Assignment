#pragma once
#include <iostream>
#include <string>

// Parses a txt file for exculsively words and returns an array of words
char** parseTxtAsWords(int lineStart, int lineEnd, std::string filePath) {

}